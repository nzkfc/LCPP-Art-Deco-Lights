1. Extract the folder
2. Drag it over you exiting liberty_city folder that contains the main LCPP files
3. Replace every file (don't worry, I included backups of each file, marked by _BACKUP for the file extension.)
4. Run you FiveM server
